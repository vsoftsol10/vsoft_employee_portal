// src/components/Spinner.jsx
import React from 'react';
import './Spinner.css'; // Import the updated CSS file

const Spinner = () => {
  return (
    <div className="spinner-container">
      <span className="loader"></span>
    </div>
  );
};

export default Spinner;
